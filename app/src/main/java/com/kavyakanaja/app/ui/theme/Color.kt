package com.kavyakanaja.app.ui.theme

import androidx.compose.ui.graphics.Color

// ── Light Theme ────────────────────────────────────────────────────────────
val SaffronOrange = Color(0xFFE07B39)     // Primary — warm saffron
val DeepKumkum = Color(0xFFC0392B)        // Secondary — deep red
val CreamParchment = Color(0xFFF8F1E3)    // Background — old paper feel
val WarmBrown = Color(0xFF5D4037)         // On-surface text
val GoldenAccent = Color(0xFFD4A017)      // Accent — gold

// ── Dark Theme ─────────────────────────────────────────────────────────────
val DarkSurface = Color(0xFF1C1008)       // Very dark warm brown
val DarkBackground = Color(0xFF13100A)
val LightSaffron = Color(0xFFFFB74D)      // Primary in dark mode
val LightCream = Color(0xFFF5DEB3)        // Text in dark mode